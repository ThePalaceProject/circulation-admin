{
  "version": "1.8.2",
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2019",
    "outDir": "lib",
    "lib": ["es2023.array", "es2019", "dom"],
    "rootDir": ".",
    // Test-framework types (mocha, jest) are declared in tsconfig.mocha.json and
    // tsconfig.jest.json respectively, to prevent duplicate global declarations
    // between the two frameworks (TS2403: describe, it, test, beforeEach, etc.).
    "skipLibCheck": false,
    // When resolving imports that come from npm-linked packages (symlinks),
    // use the symlink's location for further node_modules resolution rather than
    // following the link to the real path. This ensures all packages see a single
    // shared copy of react / @types/react from circulation-admin's node_modules.
    "preserveSymlinks": true,
    "esModuleInterop": true,
    "types": ["node"],
    "jsx": "react"
  },
  "exclude": [
    "lib",
    "node_modules",
    "tests/browser",
    // Test directories each have their own tsconfig (tsconfig.mocha.json /
    // tsconfig.jest.json) with the appropriate test-framework types.
    // Excluding here keeps the base compilation free of mocha/jest globals.
    "src/**/__tests__",
    "src/testHelper.ts",
    "tests/jest"
  ],
  "typedocOptions": {
    "entryPointStrategy": "expand",
    "out": "docs"
  }
}
